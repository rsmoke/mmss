<h1>Editing Course Preferences</h1>

<%= render 'form', course_preference: @course_preference %>

<div class="mt-2">
  <%= link_to 'Back', enrollment_course_preferences_path(@current_enrollment), class: "btn btn-blue" %>
</div>