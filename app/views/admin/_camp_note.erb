<% Campnote.all.each do |cn| %>
  <% if (cn.opendate..cn.closedate).include? DateTime.now %>
      Starting: <%= (cn.opendate).strftime("%a, %b %-d, %Y at %l:%M %p") %>
      <br>Ending: <%= (cn.closedate).strftime("%a, %b %-d, %Y at %l:%M %p") %>
      <br><%= cn.note %>
  <% end %>
<% end %>
